export enum SpellComponent {
    V = "V",
    S = "S",
    M = "M"
}

export enum AreaOfEffect {
    SPHERE = "sphere",
    CONE = "cone",
    CYLINDER = "cylinder",
    LINE = "line",
    CUBE = "cube"

}
